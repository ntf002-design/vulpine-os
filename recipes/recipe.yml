---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: vulpine-os
# description will be included in the image's metadata
description: This is my personal OS image.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/blue-build/base-images/fedora-base-nvidia
image-version: 44 # You can pin to a specific version of Fedora as well

platforms:
  - linux/amd64
  - linux/arm64
modules:
  - from-file: base/common.yml
  - from-file: base/desktop.yml
  #- from-file: base/nvidia.yml
  - type: os-release
    properties:
      PRETTY_NAME: Vulpine OS (WIP) #BlueBuild OS for Nvidia (FROM Fedora base-atomic)
  - from-file: base/post-build.yml

